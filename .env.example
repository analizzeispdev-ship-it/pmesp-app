# MongoDB
MONGO_USER=admin
MONGO_PASSWORD=pmesp2024
MONGO_DB=pmesp

# Nuxt 3 runtime config — prefixo NUXT_ é obrigatório para injeção em runtime
JWT_SECRET=change_this_to_a_secure_random_string_in_production

# Frontend (baked no build do Docker)
VITE_API_URL=http://localhost:4000
